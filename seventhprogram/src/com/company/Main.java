package com.company;
import java.util.Scanner;

import static com.company.util.Print.*;

public class Main {

    public static void main(String[] args) {
        //new Music().main(args);//изучение абстрактного класса

        //new Music1().main(args);//изучение интерфейса

        //new Apply().main(args);//отделение интерфейса от реализации с помощью обычных классов

        //new NewApply().main(args);//отделение интерфейса от реализации с помощью интерфейса и абстрактного класса

        //new FilterProcessor().main(args);//насследование с применением интерфейса, абстрактного класса, паттерна адаптер

        /*Scanner s = new Scanner(new RandowWords(10));
        while (s.hasNext())
            print(s.next()); //реализация интерфейса для выполнения требования метода

        Scanner s1 = new Scanner(new AdaptedRandomDoubles(7));
        while (s1.hasNext())
            print(s1.next() + " ");//  */

        //new Factories().main(args);//паттерн фабрика

        //new Games().main(args);//паттерн фабрика



    }
}
